{
  "version": "2.0.3",
  "visibility_values": {
    "surface": "用户直接感知的主干成品或主干输入",
    "bridge": "主干上承上启下的半成品，需要交接、缺字段或低置信度时才外显",
    "internal": "辅助模块或系统内部件，默认不向用户展示字段"
  },
  "assets": {
    "inquiry-question-card": {
      "produced_by": ["eva-learn"],
      "valid_next": ["eva-learn", "eva-think", "eva-memory"],
      "required_fields": ["asset_type", "source_module", "core_content", "user_question", "valid_next", "saved"],
      "is_handoff": false,
      "visibility": "internal"
    },
    "reading-lineage-card": {
      "produced_by": ["eva-learn"],
      "valid_next": ["eva-learn", "eva-think", "eva-memory"],
      "required_fields": ["asset_type", "source_module", "core_content", "user_question", "valid_next", "saved"],
      "is_handoff": false,
      "visibility": "internal"
    },
    "learning-progress-card": {
      "produced_by": ["eva-learn", "eva-memory"],
      "valid_next": ["eva-learn", "eva-memory"],
      "required_fields": ["asset_type", "source_module", "core_content", "user_question", "valid_next", "saved"],
      "is_handoff": false,
      "visibility": "internal"
    },
    "judgment-version-card": {
      "produced_by": ["eva-learn", "eva-think"],
      "valid_next": ["eva-think", "eva-create", "eva-memory"],
      "required_fields": ["asset_type", "source_module", "core_content", "user_question", "evidence", "valid_next", "saved"],
      "is_handoff": true,
      "visibility": "internal"
    },
    "thought-seed-card": {
      "produced_by": ["eva-learn", "eva-think"],
      "valid_next": ["eva-think", "eva-create", "eva-link", "eva-memory"],
      "required_fields": ["asset_type", "source_module", "core_content", "user_question", "evidence", "valid_next", "saved"],
      "is_handoff": true,
      "visibility": "bridge"
    },
    "core-judgment-card": {
      "produced_by": ["eva-think"],
      "valid_next": ["eva-create", "eva-memory"],
      "required_fields": ["asset_type", "source_module", "core_content", "user_question", "evidence", "valid_next", "saved"],
      "is_handoff": true,
      "visibility": "bridge"
    },
    "user-question-card": {
      "produced_by": ["eva-think"],
      "valid_next": ["eva-create", "eva-memory"],
      "required_fields": ["asset_type", "source_module", "core_content", "user_question", "evidence", "valid_next", "saved"],
      "is_handoff": true,
      "visibility": "bridge"
    },
    "idea-card": {
      "produced_by": ["eva-think", "eva-memory"],
      "valid_next": ["eva-create", "eva-memory"],
      "required_fields": ["asset_type", "source_module", "core_content", "user_question", "evidence", "valid_next", "saved"],
      "is_handoff": true,
      "visibility": "bridge"
    },
    "audience-card": {
      "produced_by": ["eva-create"],
      "valid_next": ["eva-create", "eva-link", "eva-memory"],
      "required_fields": ["asset_type", "source_module", "core_content", "user_question", "evidence", "valid_next", "saved"],
      "is_handoff": true,
      "visibility": "bridge"
    },
    "title-handoff-card": {
      "produced_by": ["eva-create"],
      "valid_next": ["eva-create", "eva-memory"],
      "required_fields": ["asset_type", "source_module", "core_content", "user_question", "evidence", "valid_next", "saved"],
      "is_handoff": true,
      "visibility": "bridge"
    },
    "opening-handoff-card": {
      "produced_by": ["eva-create"],
      "valid_next": ["eva-create", "eva-memory"],
      "required_fields": ["asset_type", "source_module", "core_content", "user_question", "evidence", "valid_next", "saved"],
      "is_handoff": true,
      "visibility": "bridge"
    },
    "commercial-constraint-card": {
      "produced_by": ["eva-create", "eva-brief"],
      "valid_next": ["eva-create", "eva-brief", "eva-link"],
      "required_fields": ["asset_type", "source_module", "core_content", "user_question", "evidence", "valid_next", "saved"],
      "is_handoff": true,
      "visibility": "bridge"
    },
    "content-task-card": {
      "produced_by": ["eva-create", "eva-brief"],
      "valid_next": ["eva-create", "eva-link"],
      "required_fields": ["asset_type", "source_module", "core_content", "user_question", "evidence", "valid_next", "saved"],
      "is_handoff": true,
      "visibility": "bridge"
    },
    "content-asset-card": {
      "produced_by": ["eva-create", "eva-link"],
      "valid_next": ["eva-memory"],
      "required_fields": ["asset_type", "source_module", "core_content", "user_question", "evidence", "valid_next", "saved"],
      "is_handoff": true,
      "visibility": "surface"
    },
    "review-card": {
      "produced_by": ["upgrade-pending-review"],
      "valid_next": ["eva-think", "eva-create", "eva-memory"],
      "required_fields": ["asset_type", "source_module", "core_content", "user_question", "evidence", "evidence_level", "path_bottleneck", "design_type", "treatment_variable", "outcome_variable", "control_plan", "adjustment_variables", "hypothesis", "confounders", "metric_spec", "next_test_action", "metrics_to_watch", "success_criteria", "observation_window", "result_backfill_required", "valid_next", "saved"],
      "is_handoff": true,
      "visibility": "bridge"
    },
    "persona-card": {
      "produced_by": ["eva-memory"],
      "valid_next": ["eva-create", "eva-link"],
      "required_fields": ["asset_type", "source_module", "core_content", "user_question", "evidence", "valid_next", "saved"],
      "is_handoff": true,
      "visibility": "bridge"
    },
    "voice-card": {
      "produced_by": ["eva-memory"],
      "valid_next": ["eva-create", "eva-link"],
      "required_fields": ["asset_type", "source_module", "core_content", "user_question", "evidence", "valid_next", "saved"],
      "is_handoff": true,
      "visibility": "bridge"
    }
  }
}
