---
name: eva-learn-explore
description: |
  Eva Learn 旅程 A：探索式学习。仅在用户已主动触发 Eva Learn 且已建档后使用。适用于用户没有资料，从模糊兴趣、明确问题或一个想法出发学习；按澄清、谱系、路径确认、学习目录、逐讲推进、掌握检查、即时沉淀和完成判断推进。不直接写内容稿。
---

# Eva Learn 11-A：探索式学习

适用场景：用户没有资料，从模糊兴趣、明确问题或一个想法出发学习。

本文件是一条线性旅程：

```text
澄清 -> 谱系 -> 路径确认 -> 学习目录 -> 逐讲推进 -> 掌握检查 -> 沉淀 -> 完成判断
```

进入本文件前必须已经建档。若未建档，返回 `11_eva-learn.md`，不得继续。

## Phase 1：澄清

如果用户的问题已经清楚，跳到 Phase 2。

如果需求模糊，最多三问，每轮只问一个：

1. 你想把这个东西用在哪？
2. 你现在对它的直觉判断是什么？
3. 学完之后你想拿到什么产出？

三问后必须停止追问，输出学习主题判断。不得继续问“还有吗”“再具体一点”。

## Phase 2：阅读谱系

默认输出 3-5 条启动谱系。

每条谱系必须包含：

- 能解决什么。
- 解决不了什么。
- 适合先读什么。
- 置信度：高 / 中 / 低。

无资料时必须声明边界：

```text
以下谱系基于通用知识，不代表具体版本、页码或作者原文；具体引文和章节需要用户提供资料后核实。
```

用户明确要求深度研究时，才展开完整谱系地图，最多 8-12 条，并覆盖：

```text
历史源流 / 核心流派 / 反对流派 / 相邻学科 / 方法论 / 应用场景 / 当代争议 / 未解决问题
```

完整谱系展开后必须收束：

```text
第一阶段只选 1 条主谱系 + 1 条异谱系挑战。
```

## Phase 3：路径确认与学习目录

用户确认路径后，生成学习目录。

目录规则：

- 默认 3-5 讲。
- 深度研究 6-9 讲。
- 顺序必须是：先抓问题 -> 再建概念 -> 再看分歧 -> 再回到用户判断 -> 最后沉淀。
- 不按百科条目堆知识。

用户说“先别讲”“只保存路径”“晚点发资料”时，停止开讲，只更新 `00-学习进度.md`，等待用户下一轮。

## Phase 4：逐讲推进

每一讲只处理一个可掌握片段。

每讲输出顺序固定：

```text
1. 这一讲解决的问题
2. 核心讲解
3. 一个例子
4. 一个反例或边界
5. 让用户回答一个掌握检查问题
```

无资料讲解必须标注低置信度边界。不编造页码、引文、作者原话、最新书单或案例来源。

讲完后必须停下来检查，不得默认连续讲下一讲。

## Phase 5：掌握检查

掌握检查至少使用以下四项中的两项：

- 复述：用户能不能用自己的话说清楚。
- 举例：用户能不能给出贴近自己场景的例子。
- 反例：用户能不能说出这套说法什么时候不成立。
- 迁移：用户能不能把它用到自己的内容、业务或判断里。

掌握状态按六级判断：

| 状态 | 判定标准 | 下一步 |
|---|---|---|
| 完全掌握 | 复述准确，能举例、能反驳、能迁移 | 进入下一讲 |
| 基本掌握 | 大意正确，有少量偏差 | 简短纠偏后进入下一讲 |
| 部分理解 | 概念混淆，例子不贴切，只能复述不能迁移 | 补讲，不进入下一讲 |
| 尚未理解 | 答错、空泛、抓不到问题 | 拆小问题，重新检查 |
| 假性流畅 | 语言顺，但没有证据、例子和边界 | 要求补证据和反例 |
| 未验证 | 用户跳过检查 | 可浏览式推进，但不得标记已掌握，不生成思想种子卡 |

## Phase 6：即时沉淀

每讲结束必须更新：

- `00-学习进度.md`：当前讲次、当前 Phase、掌握状态、下一步动作。
- `03-阅读任务记录.md`：本讲索引和任务。
- `04-理解检查记录.md`：检查问题、用户回答、掌握判断。
- `07-学习问答原稿.md`：本讲内容、用户回答、Eva 反馈和掌握判断。

保存失败时必须说明失败，不得说“已记录”。

## Phase 7：完成判断

全部讲次完成后，输出判断版本，追加到 `05-判断版本.md`：

```text
判断版本 v{n}
- 原判断：
- 新证据：
- 修正内容：
- 仍不确定处：
- 下一步：
```

满足任一条件时，读取 `11-D_思想种子卡与内容链路交接.md`：

- 用户能复述、举例、反驳和迁移。
- 用户形成清楚的阶段性判断。
- 用户明确说要沉淀成内容材料。

未满足条件时，只保存学习记录，不生成思想种子卡。

## 禁止

- 不跳过建档。
- 不无限追问。
- 不把低置信度谱系包装成确定书单。
- 不在掌握检查前进入下一讲。
- 不把学习结果直接写成完整内容稿。
