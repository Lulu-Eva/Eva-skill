---
name: eva-learn-project
description: |
  Eva Learn 子模块：学习项目与资料存档。仅在用户已主动触发 eva-learn 后使用。用于新建或恢复长期学习项目，保存学习进度，复制用户提供的文档、书摘、PDF、图片、课程材料或本地文件到 sources/原始资料/，并维护资料索引与进度文件。它只保存进度和资料，不生成连续课程文章。
---

# Eva Learn 子模块：学习项目与资料存档

本文件只处理学习项目、资料存档和进度恢复。

不要在本文件里执行提问式学习、主题式阅读、理解检查或内容转化；这些分别交给 `11b` 和 `11c`。

## 使用时机

仅在用户已经主动触发 `eva-learn` 后读取本文件。

读取本文件的场景：

- 新主题需要多轮阅读、主题研究或下次继续。
- 用户说“继续 eva-learn”“接着上次读”“继续这个主题”。
- 用户提供书摘、文章、课程材料、PDF、图片或本地文件路径。
- 当前任务需要保存阅读任务、理解检查、判断版本或思想种子卡。

短问题、一次性带读、没有后续继续意图时，可以不建学习项目文件夹，只在对话里输出本轮任务。

## 建档判定

必须建档：

- 用户说“长期学、下次继续、接着读、主题研究、多本书、保存进度”。
- 用户提供本地文件、PDF、多份材料，并希望围绕它持续学习。
- 用户进入主题式阅读，且预计超过 1 轮。
- 用户说“继续 eva-learn”，但当前主题还没有进度文件。

不必建档：

- 一次性问一本书值不值得读。
- 只要一个低置信度阅读路径。
- 只做一次短材料带读，没有后续继续意图。
- 用户明确说“不用保存”。

如果是否建档不清楚，只问一个问题：

```text
这次要不要建一个 Eva Learn 学习项目，方便你下次接着读？
```

## 建档执行协议

需要建档时，必须按以下顺序执行。不得只在对话里说“已保存”，却没有真实创建文件或记录状态。

```text
1. 确定主题名。
2. 定位保存目录。
3. 创建固定文件结构。
4. 处理用户提供的资料。
5. 更新 sources/INDEX.md。
6. 更新 00-学习进度.md。
7. 告知用户保存路径、资料状态和下一步。
```

### 1. 确定主题名

主题名优先级：

1. 用户明确给出主题名：使用用户原主题名，去掉不适合作为文件夹名的符号。
2. 用户给出问题：压缩成 8-20 个字的主题名。
3. 用户只给书名/资料名：使用书名或资料名。
4. 无法判断：只问一个问题。

```text
这个 Eva Learn 项目我先用哪个主题名保存？
```

不要使用“新建项目”“学习资料”“未命名”作为长期主题名。

### 2. 定位保存目录

按“存放位置”规则确定目录后，先检查是否已存在同名主题文件夹：

- 已存在：读取 `00-学习进度.md`，判断是继续旧项目还是新建同名变体。
- 不存在：创建新主题文件夹。
- 同名但主题明显不同：在主题名后追加日期或序号。

如果无法确认是否同一主题，只问：

```text
我找到了一个同名 Eva Learn 项目。你是要接着它学，还是新建一个同名新项目？
```

### 3. 创建固定文件结构

新建项目时，必须创建：

```text
00-学习进度.md
01-探究问题卡.md
02-阅读谱系.md
03-阅读任务记录.md
04-理解检查记录.md
05-判断版本.md
06-思想种子卡.md
sources/INDEX.md
sources/原始资料/
```

如果部分文件已存在，不覆盖，只补齐缺失文件。

### 4. 处理用户提供的资料

- 本地文件路径：复制到 `sources/原始资料/`。
- 粘贴长文本：保存为 `sources/原始资料/{日期}-粘贴资料.md`。
- 图片、PDF、课件或附件：复制到 `sources/原始资料/`。
- 无法复制或无法写入：不假装成功，在 `sources/INDEX.md` 标记为“待补档”，并在回复里说明。

### 5. 更新资料索引

每处理一份资料，都要追加或更新 `sources/INDEX.md`。

资料编号按 `S1`、`S2`、`S3` 递增；不得复用旧编号指向新资料。

### 6. 更新学习进度

建档完成后，必须更新 `00-学习进度.md` 至少以下字段：

```markdown
- 当前问题：
- 当前阶段：
- 当前材料：
- 当前资料编号：
- 最近一次阅读任务：
- 下一步：
- 最近更新：
```

如果本轮还没有阅读任务，`最近一次阅读任务` 写“待生成”，不要空着。

### 7. 回复用户

建档后回复只说三件事：

```markdown
## 已建档

- 主题：
- 保存位置：
- 已存资料：

## 下一步

{一个阅读或提问动作}
```

如果没有真实写入文件，必须改成：

```markdown
## 这次还没保存成功

- 原因：
- 我先在对话里保留的进度：
- 你可以指定一个可写入目录，我再补建档。
```

## 学习项目原则

Eva Learn 可以跨多次对话推进。用户不需要一次读完所有材料。

学习项目文件夹只记录：

- 现在研究什么问题。
- 这轮读什么材料。
- 用户已经读到哪里。
- 用户的原始判断和判断修正。
- 下一次回来应该从哪里继续。

不要把它做成连续课程文章，不生成 `01.md`、`02.md` 这类课程文件。

## 存放位置

目录优先级：

1. 用户明确指定目录：使用用户指定目录。
2. 当前目录是项目目录：使用 `当前目录/eva-learn/{主题名}/`。
3. 当前目录不适合保存：使用 `~/Documents/eva-learn/{主题名}/`。

项目目录判断：

- 当前目录有 `AGENTS.md`、`README.md`、`.git`、`skills/`、`知识库/` 任一项，即视为项目目录。
- 如果当前目录是 Eva-Skill 仓库本体、安装后的 `skills/eva/` 目录，或明显属于 Skill 源码目录，不在其中创建学习项目；改用用户指定目录或 `~/Documents/eva-learn/{主题名}/`。

如果无法写入目标目录，不要假装已经保存。直接告诉用户：

```text
这次我先把学习进度输出在对话里。当前目录暂时无法写入，等你指定可保存的位置后，我再帮你建档。
```

## 文件结构

```text
{主题名}/
├── 00-学习进度.md
├── 01-探究问题卡.md
├── 02-阅读谱系.md
├── 03-阅读任务记录.md
├── 04-理解检查记录.md
├── 05-判断版本.md
├── 06-思想种子卡.md
└── sources/
    ├── INDEX.md
    └── 原始资料/
```

## 资料存档

如果用户提供文档资料、书摘、文章、课程材料、PDF、图片或本地文件路径，应尽量把原始资料复制或保存到当前学习项目的 `sources/原始资料/` 中，方便下次续学。

资料存档规则：

- 用户提供本地文件路径：复制原文件到 `sources/原始资料/`，保留原文件名。
- 用户粘贴长文本：保存为 `sources/原始资料/{日期}-粘贴资料.md`。
- 用户提供图片、PDF、课件或其他附件：复制到 `sources/原始资料/`，不改动原文件内容。
- 同名文件已存在时，不覆盖，文件名后追加日期或序号。
- 如果当前环境无法复制文件，不要假装已保存；在 `sources/INDEX.md` 记录原路径和“待补档”状态。

`sources/INDEX.md` 固定记录：

```markdown
# 资料索引

| 编号 | 资料名 | 类型 | 来源 | 存档位置 | 用途 | 状态 |
|---|---|---|---|---|---|---|
| S1 |  | 书籍/文章/PDF/课程/摘抄/图片 | 用户上传/本地路径/粘贴 |  | 用来回答哪个问题 | 已存档/待补档 |
```

每次布置阅读任务时，必须标注使用了哪些资料编号，例如 `S1`、`S2`。

## 进度文件模板

`00-学习进度.md` 固定记录：

```markdown
# {主题名}｜Eva Learn 学习进度

## 当前状态

- 当前问题：
- 当前阶段：问题澄清 / 阅读中 / 等待用户读完 / 理解检查 / 判断修正 / 可转表达
- 当前材料：
- 当前资料编号：
- 最近一次阅读任务：
- 下一步：
- 最近更新：

## 已完成

- 

## 未解决问题

- 

## 判断版本索引

| 版本 | 日期 | 核心修正 | 下一步 |
|---|---|---|---|
| v1 |  |  |  |
```

## 保存时机

每次完成以下动作后，都要更新 `00-学习进度.md`：

- 用户提供新的文档资料、书摘、课程材料或本地文件。
- 生成探究问题卡。
- 生成阅读谱系。
- 布置阅读任务。
- 完成理解检查。
- 生成判断版本。
- 生成思想种子卡。

对应内容追加到同名记录文件，不要覆盖旧版本。

## 恢复进度

当用户说“继续 eva-learn”“接着上次读”“继续这个主题”时：

1. 先找到对应主题文件夹。
2. 读取 `00-学习进度.md`。
3. 读取 `sources/INDEX.md`，确认本主题有哪些原始资料。
4. 读取最近一条阅读任务或判断版本。
5. 用 3-5 条告诉用户当前进度和当前正在使用的资料。
6. 只推进下一步，不从头开始。

如果找不到对应主题文件夹，只问一个问题：

```text
我没找到这个 Eva Learn 主题的进度文件。你想继续哪个主题？如果有保存目录，也可以直接发给我。
```

## 禁止

- 不把学习项目文件夹做成课程生成器。
- 不自动生成连续学习文章。
- 不覆盖旧资料、旧判断版本或旧任务记录。
- 不把泛泛读书摘要保存成思想种子卡。
